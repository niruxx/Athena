#pragma once

// Bump this alongside each tagged GitHub release (tag vX.Y.Z) so
// GitHubReleaseChecker can tell whether the latest release is newer than
// the running build.
#define ATHENA_VERSION "2.0.0"
